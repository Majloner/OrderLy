SUPABASE_URL=###
SUPABASE_KEY=###
# Service-role key — server-only. Used to mint menu-photo signed upload URLs and
# remove objects (Storage does not honor the user JWT for RLS in this project).
SUPABASE_SERVICE_ROLE_KEY=###
