---
import Layout from "@/layouts/Layout.astro";

// display_name, not user.email — staff sign in under a synthetic address.
const { user, role, display_name } = Astro.locals;
const isOwner = role === "owner";
---

<Layout title="Panel">
  <div class="bg-background flex min-h-screen items-center justify-center p-4">
    <div class="rounded-2xl border border-white/10 bg-white/10 p-8 text-center text-white backdrop-blur-xl">
      <h1 class="font-display text-primary mb-4 text-3xl font-bold">Panel</h1>
      <p class="text-blue-100/80">
        Witaj, <span class="font-semibold text-white">{display_name ?? user?.email}</span>
      </p>
      <p class="mt-2 text-sm text-blue-100/50">Ta strona jest tylko dla zalogowanych.</p>
      <div class="mt-6 flex justify-center gap-3">
        {
          isOwner && (
            <a
              href="/menu"
              class="inline-block rounded-lg border border-white/20 bg-white/10 px-4 py-2 text-sm transition-colors hover:bg-white/20"
            >
              Menu
            </a>
          )
        }
        {
          isOwner && (
            <a
              href="/staff"
              class="inline-block rounded-lg border border-white/20 bg-white/10 px-4 py-2 text-sm transition-colors hover:bg-white/20"
            >
              Pracownicy
            </a>
          )
        }
        {
          isOwner && (
            <a
              href="/room"
              class="inline-block rounded-lg border border-white/20 bg-white/10 px-4 py-2 text-sm transition-colors hover:bg-white/20"
            >
              Schemat sali
            </a>
          )
        }
        <a
          href="/settings"
          class="inline-block rounded-lg border border-white/20 bg-white/10 px-4 py-2 text-sm transition-colors hover:bg-white/20"
        >
          Ustawienia lokalu
        </a>
      </div>
      <form method="POST" action="/api/auth/signout" class="mt-4">
        <button
          type="submit"
          class="rounded-lg border border-white/20 bg-white/10 px-4 py-2 text-sm transition-colors hover:bg-white/20"
        >
          Wyloguj
        </button>
      </form>
    </div>
  </div>
</Layout>
